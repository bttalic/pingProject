# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.ProductController.index()
GET     /products/new				controllers.ProductController.create()
POST    /products/new               controllers.ProductController.newProduct()
POST    /products/update			controllers.ProductController.updateProduct()


# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
